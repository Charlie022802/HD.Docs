# 看片端授權機制設計（HD.DicomImageViewer）

**一句話**：綁機器、簽章驗證的基本防護，擋「整包複製到別台就能跑」；簽發永遠留在原廠，
但**申請與派發走醫院主機的資料庫**，不必再為了拿檔案跑現場。

- **狀態**：2026-08-14 **實作完成，端到端驗證通過**（線上與離線兩條路都有）。
  **目前是蒐集階段——沒有授權不會被擋**，見下方「上線策略」。
- **範圍**：嚇阻級，不是 DRM（見文末「這個做不到什麼」）。
- 相關：[systems/viewer.md](systems/viewer.md)、[systems/admin-console.md](systems/admin-console.md)、[viewer-install-design.md](viewer-install-design.md)

---

## 為什麼要做

Keycloak／管理主控台尚未推到醫院端，現階段沒有任何機制阻止把安裝好的資料夾複製到另一台電腦執行。

原本的設計是純離線：看片端匯出 `.req`，同事帶回原廠簽，再帶授權檔回現場。
真正的痛點不在簽章，而是**醫師的個人電腦我們連不到，拿檔案一定要跑一趟**。

關鍵觀察是：**每間醫院一定會裝一台我們的主機（PACS + DB），而看片端本來就要連那台
PostgreSQL 才能看片——能看片就一定連得到 DB**。所以直接拿資料庫當信箱，
不必開埠、不必新服務、不必在每間醫院部署主控台。

---

## 兩個獨立的問題

設計時最容易搞混的地方：「申請檔怎麼送」與「誰拿著私鑰」是**兩件事**。

| | 決定 | 理由 |
|---|---|---|
| **傳輸** | DB 當信箱（連不上時退回離線檔案） | 看片端本來就連得到 DB；零額外基礎建設 |
| **簽發** | 私鑰只在原廠 `.191`，我們 VPN 進去按簽發 | 醫院端若能簽發，等於自己可以無限發 |

拿 DB 當信箱**不會降低安全性**：寫回去的必須是原廠私鑰簽過的字串，自己塞的驗不過。
那張表只是郵筒，不是核准權。

---

## 決策一覽

| 項目 | 決定 | 理由 |
|---|---|---|
| 授權粒度 | 按台，一機一份 | 與現行銷售與作業方式一致 |
| 簽發位置 | 原廠 `.191` 主控台，私鑰不離開 | 同上 |
| 期限 | 預設永久；欄位保留給 demo | 醫院都是賣斷 |
| 驗證方式 | 客戶端只有公鑰，只驗簽章 | 舊版把私鑰放客戶端＝可偽造 |
| 演算法 | ECDSA P-256（ES256） | .NET 原生、簽章 64 bytes、無 RSA padding 坑 |
| 指紋 | 四取二；只中一個仍放行並提醒 | 韌體／OS 更新曾造成「突然不能用」 |
| 授權檔位置 | `%ProgramData%\HyperDigital\DicomImageViewer\license.lic` | 不能放版本目錄（更新換位置、清舊版會刪掉） |
| 強制與否 | **編譯期常數**，目前 `false` | 設定檔開關等於在現場附一把關掉授權的鑰匙 |

---

## 上線策略：先蒐集，再強制

`LicenseGate.Enforce` 目前是 `false`。這一版程式照樣送指紋進 DB、能取回授權就落地，
**但沒有授權也不影響使用，而且完全不跳視窗**。

現場既有的看片端全都是「從來不必註冊」的狀態，一上版就開始擋，等於我們自己把所有醫院鎖住。

```
2.4.x（Enforce = false）  出貨 → 清冊自動長出來 → 我們把在用的機器全簽掉
2.5.x（Enforce = true）   才開始擋；切換當下沒有人會被擋到
```

刻意做成編譯期常數而不是 `localconfig.json` 的選項：設定檔裡放一個「把授權關掉」的開關，
比不做還糟。

### 怎麼產生「強制模式」的測試版（2026-08-30）

```bash
dotnet publish ... -c Release -p:EnforceLicense=true
```

那個參數會定義組建符號 `HD_LICENSE_ENFORCE_BUILD`，並在版本號尾巴加上 `.ENFORCE`：

```
一般   2.4.0+20260830-230025+0800
強制版 2.4.0+20260830-230057+0800.ENFORCE
```

**兩個性質都要**：

1. **正式流程產出的組件裡根本不存在那條路** —— 它在 `#if` 裡，符號沒定義就不會被編譯，
   不是「有個旗標是 `false`」。所以正式版不可能因為誰改錯設定而開始擋人。
2. **版本號自己講出來** —— 否則手上那顆 tgz 是哪一種，從檔名、從「關於」視窗都看不出來。
   三個月後有人拿它去裝，就會擋到不該擋的站台。

驗證方式：組件裡有沒有「這是強制模式的組建」那段字串（UTF-16）。
啟動時 log 也會警告，授權判定那行會標「／強制模式」。

原本 DEBUG 組建吃 `HD_LICENSE_ENFORCE=1` 那條保留不動——快速自測用。

### ⚠️ 部署前提：`Enforce = true` 的版本不能進到「沒有 `HD_DEVICE_LICENSE`」的站台

> **2026-08-31 更正：判準是「表在不在」，不是版本號。**
> 原本這一節寫成「DB `< v2.0.29` 就不行」，並舉若瑟為例。實際查過之後發現
> **若瑟（`v2.0.22`）那張表是存在的** —— 2026-08-19 前後被單獨補上去，沒走更新鏈
> （同期的 `SITE`／`PACKAGE_JOB`／`HD_USER_AUDIT_LOG`／`UPS_WORKITEM` 都還沒有，
> 所以確實只補了這一張）。**用版本號判斷會得到假的否定。**
> 這就是[更新鏈分岔](josef-db-upgrade-plan.md)的形狀：結構變更存在於某些機器上，
> 卻沒有經過腳本，於是版本號與實際結構對不起來。

`HD_DEVICE_LICENSE` 名義上是 **`db_update_v2.0.29.sql`** 建的，但**不能反過來用版本號推**——
若瑟停在 `v2.0.22` 卻有那張表。真正要問的是表在不在；沒有的話：

```
查不到自己那列（表不存在，被 catch 安靜吞掉）
  → SubmitRequest 也寫不進去
  → 走離線流程，在本機建 DeviceId + RequestedAt
  → 暫用期（14 天）
  → 期滿 · Enforce=true · 擋下
  → 而「怎麼註冊」這條路根本不存在
```

不是永久鎖死——重灌會刷新本機的 `RequestedAt` 再拿一次暫用期。但那等於要現場**定期重灌看片端**。

**危險在於症狀離原因很遠**：現象是「裝上去兩週後醫師突然登不進去」，而沒有人會聯想到是
資料庫少了一張表。所以這條要當**出貨前的檢查項**，不是靠記憶。

**檢查方式**（在目標站台的 DB）：

```sql
SELECT to_regclass('public."HD_DEVICE_LICENSE"');   -- NULL 就是不能出強制版
```

**目前已知的實況（2026-08-31 實際查過三個站台）**：

| 站台 | DB 版本 | `HD_DEVICE_LICENSE` | `APP_VERSION` 寬度 |
|---|---|---|---|
| `.191` | `2.0.41` | 有 | **64**（已套 `v2.0.41`） |
| `.163` | `2.0.26` | 有 | 32 |
| 若瑟 `10.10.1.148` | `2.0.22` | **有**（單獨補的） | 32 |

**三台都有那張表**，所以強制版目前對三者都不會踩到「無法註冊」那條路。
但三台的 `APP_VERSION` 寬度不同 —— 客戶端因此要能同時對付 32 與 64，
見下方「版本字串的欄位寬度」。

---

## 什麼時候檢查：**登入成功之後，不是程式啟動時**

這點一開始做錯了。閘門原本放在 `Program.Main`，但**資料庫位址是使用者在登入畫面的齒輪
（啟動設定 → 資料庫連線）裡填的**，而全新安裝的 `localconfig.json` 位址刻意留空。
結果是：第一次啟動必然走「連不上 DB」那條路，而且因為 `PostgresSettings` 的預設 Host 是
`127.0.0.1`，會**卡滿 10 秒連線逾時**才出現登入畫面——使用者連填設定的機會都還沒有。

改掛在 `LoginForm.LogIn()` 裡「帳號驗過（查 DB）、`RefreshConfigs()` 也成功」之後：
那是全程第一個**資料庫確定連得上**的時間點，正是線上註冊需要的前提。

- 機制本身留在 exe 那一層（需要 `System.Management`），Core 只留一個 `Func<bool>` 掛鉤，沒掛＝不檢查
- 驗一次就好——登入失敗重試很常見，不該每按一次就重跑一輪
- 不放行時停在登入畫面，而那裡正好就是改設定與重試的入口
- **媒體模式（病患光碟）走另一條路，不經過這裡**，本來就不該要授權

實測：資料庫不通時登入畫面 1.43 秒出現，日誌裡一行 `[Licensing]` 都沒有。

## 判斷順序

```
1. 本機 %ProgramData% 的 license.lic ──驗簽 + 四取二──▶ 過 → 直接進去
   （這一步不碰 DB，實測 30ms）
   └ 同時在背景回報 LAST_SEEN_AT，順便看自己有沒有被撤銷

2. 沒有／驗不過 → DB 連得上 → 用 DeviceId（沒有才用指紋）找自己那一列
   ├ 已簽發 → 把 LICENSE_TEXT 抓下來落地 → 進去
   │        （重灌、重裝、授權檔誤刪都自動復原，不必找我們）
   └ 還沒簽發 → 顯示「已送出申請」

3. 從來沒送過 → INSERT 一筆 pending → 等我們 VPN 進去簽

4. DB 連不上 → 離線流程：匯出 .req／匯入授權檔（格式與線上完全相同）
```

**任何一步失敗都只是安靜退回下一條路**——註冊機制不該有能力讓看片程式開不起來。
DB 沒有 `HD_DEVICE_LICENSE` 表（舊環境沒跑 migration）也一樣，記一行 log 就走離線。

### 版本字串的欄位寬度（`APP_VERSION`）

`db_update_v2.0.41` 把 `public."HD_DEVICE_LICENSE"."APP_VERSION"` 從 `varchar(32)`
加寬到 `varchar(64)`。原因是 32 對現在的版本字串已經不夠，而**溢位是靜默的**：

```
2.4.0+20260831-100434+0800            26 字元  剛好
2.4.0+20260831-100434+0800.ENFORCE    34 字元  被截成 .ENFOR
2.5.0-beta.1+20260831-100434+0800     33 字元  掉最後一個字
```

版控規約在發布前一律用 `-alpha.N` / `-beta.N`，所以下次出交測版就會開始掉字元，
而掉的是尾端 —— 也就是分辨兩顆同語意版本的 build 唯一靠得住的部分。

**客戶端要同時對付 32 與 64。** 它先送 64，遇到 `22001 value too long` 就記住這個站台
是舊的、退回 32 重試一次（`LicenseRepository._appVersionMax`）。
不能讓 `22001` 掉進通用的 catch —— 那會讓 `ReportSeen` 回 `null`，上層當成「查不到這一列」，
於是 `LAST_SEEN_AT` 靜默停止更新，而那正是判斷「哪些機器還在用」的依據。

### 暫用期

送出申請後 **14 天**內照常使用——安裝當天我們不一定在線上，總不能讓同事在現場乾等。

> **2026-08-30 從 3 天拉長到 14 天。** 原本的 72 小時對「站台還沒準備好」的情況太短：
> DB 版本較舊的站台連申請都寫不進去（見上方部署前提），三天後就被擋。
> 兩週才有足夠緩衝把站台弄好。**這只是緩衝不是解法**，前提條件仍然要守。

線上模式以 **DB 的 `REQUESTED_AT`** 為準，不是本機時間：記在本機的話重灌就能刷新，
等於無限期試用。離線模式只能記本機，那是可接受的漏（嚇阻級）。

---

## 授權檔格式

```json
{
  "v": 1,
  "alg": "ES256",
  "typ": "license",
  "payload": "<base64url 的 payload JSON>",
  "sig": "<base64url 的簽章>",
  "chain": ["<base64url 的站台憑證>"]
}
```

payload：

```json
{
  "product": "DicomViewerStation",
  "deviceId": "<GUID>",
  "issuedAt": "2026-08-13T18:22:44Z",
  "expiresAt": null,
  "machine": { "machineGuid": "<hash>", "smbiosUuid": "<hash>",
               "baseboard": "<hash>", "cpu": "<hash>" }
}
```

三個容易踩的點，都已經處理：

1. **簽的是 payload 的原始位元組**，不是「解析後重新序列化」。後者只要兩邊 JSON 的空白、
   欄位順序、跳脫方式有一點差異就驗不過，症狀是「明明是我們簽的卻說簽章不符」，極難查。
2. **`typ` 一定要比對**（域分離）。否則一份原廠簽的裝置授權有機會被當成站台憑證餵進來。
   舊版授權檔沒有這個欄位，視為 `license`——已發出去的仍然驗得過。
3. **`chain` 現在是空的，但驗證路徑已經走通**。保留它是為了日後若要把簽發權下放到
   醫院主機（原廠簽站台憑證、站台憑證再簽裝置授權）。等授權都發出去了才實作，
   現場所有既有看片端都會驗不過，得全部重發。

**院所／科室／站別／醫師不進授權檔**——那些是清冊資訊，要能隨時改；
進了簽章 payload 就等於「改一次要重簽重發」。醫師名稱另有個資考量。

---

## 機器指紋

四個來源，各自雜湊後存入授權檔與清冊（**只存雜湊不存原值**：主機板序號那些屬於客戶的
資產資訊，而比對只需要「一不一樣」）。

| 來源 | 穩定度 | 什麼時候會變 |
|---|---|---|
| `HKLM\SOFTWARE\Microsoft\Cryptography\MachineGuid` | 高 | 重灌作業系統 |
| SMBIOS UUID（`Win32_ComputerSystemProduct.UUID`） | 高 | 換主機板 |
| 主機板序號（`Win32_BaseBoard.SerialNumber`） | 中低 | BIOS 更新可能重置 DMI |
| CPU（`Win32_Processor.ProcessorId`） | **形同無效，見下** | — |

**判定規則**

| 對中幾項 | 行為 |
|---|---|
| ≥ 2 | 正常啟動 |
| 1 | **照常啟動**＋提醒＋記 log，30 天寬限換發 |
| 1（而且那一項是 CPU） | 擋下——那是同型號的另一台機器 |
| 0 | 擋下（真的換機器了） |

硬體維修與韌體更新只會讓一兩項漂移；複製到別台是四項全不對。所以「只中一項放行」
幾乎不損失防拷貝能力，卻消除了「某天早上突然不能用」。

### CPU 那項幾乎沒有識別力（實測發現）

開發機實測 `Win32_Processor.ProcessorId` = `178BFBFF00B60F00`——那是**型號**層級的值，
同一款 CPU 的機器全都一樣，不是每顆晶片獨有的序號。

照原本的規則，把授權複製到**同型號的另一台電腦**會剛好中 CPU 一項 → 判定成漂移 → 放行。
而同型號正是醫院整批採購最常見的情況。所以把它列為**弱識別**：只中它一項時當作不同機器。

例外：若「可比對的就只有那一項」（其餘三項在某一邊根本讀不到），仍照舊放行＋提醒——
不擋人的原則優先。

### 另外兩個必須處理的細節

1. **垃圾值不算數**：`To be filled by O.E.M.`、空字串、全零 UUID 一律視為「無法取得」。
   否則兩台不同機器會在該項相符，授權變成可攜。
   **這件事一定要在客戶端雜湊之前做**——雜湊過就認不出那些字串了。
2. **WMI 讀不到不能擋人**：WMI 會逾時、丟例外、被群組原則關掉。三個 WMI 查詢並行並設
   5 秒總逾時，讀不到就少一項指紋。`MachineGuid` 走登錄檔，幾乎不會失敗。

net10 取 WMI 需加 `System.Management` NuGet，**只加在 `HD.DicomImageViewer` 這支 exe**——
`Core` 還要給 Linux 上的 Server 用。

---

## 撤銷

離線授權撤銷了客戶端根本不會知道；現在靠**每次啟動的背景回報**（`LAST_SEEN_AT`）才有實效。

回報跑在背景，發現被撤銷時畫面早就開起來了——硬把正在判讀的視窗關掉是臨床風險，
所以記在本機、**下次啟動**才處理，並給 7 天寬限期。

`LAST_SEEN_AT` 同時回答「這台到底還有沒有在用」，按台計價時清點很需要。

---

### 撤銷要等它回答（2026-08-31 修正）

原本第 1 步（本機授權檔有效）直接 `return`，回報是 fire-and-forget，所以**撤銷永遠慢一輪**。
同一份 log 裡的對照組：

```
17:31:13.600  授權判定：Valid（放行）      ← 舊行為：先放行
17:31:13.621  回報時發現授權已撤銷         ← 21ms 後才知道

18:00:37      主控台撤銷
18:00:47.813  源頭已撤銷這台裝置           ← 新行為：先問再決定
18:00:47.813  授權已撤銷，寬限期內仍放行
```

**答案不是太慢，是沒有人等它。** 現場的表現是「撤銷完那台照樣能用，要關掉再開一次才擋得住」。

「發現時畫面已經開起來了、硬關正在判讀的視窗是臨床風險」這個理由**對執行中成立，
對閘門不成立**——閘門跑在登入按鈕上、主畫面還沒開。而且閘門跑在一次成功的登入之後，
同一台伺服器幾百毫秒前才回應過，所以多問這一趟不會是連不上的風險來源。
實測代價 **14ms**（整個判定 47～52ms，原本背景版 27ms），登入本身是 400ms 等級。

### 撤銷不能是單向的

本機 `device.json` 的 `RevokedAt` 原本只會被寫入、**沒有任何地方清掉**，而第 1 步一看到它
就攔截。後果是主控台重新簽發之後客戶端永遠回不來——**連「匯入授權檔」都救不回來**
（新授權檔寫進去了，撤銷記號還在），現場唯一的解法會變成「手動去 `%ProgramData%` 刪檔」。

現在 `SyncRevocation` 兩個方向都做。**問不到就維持現狀**：連不上或查不到那一列都不解除
也不新增——拔掉網路不能等於解除撤銷，網路一抖也不能把所有人鎖在門外。

### 主控台要有「重新啟用」

清單上已撤銷的列原本只剩一個徽章。但重新啟用**明明是預期存在的能力**（簽發那條路特地
處理了 `targetRevoked`，SQL 也明確把 `REVOKED_AT` 清成 NULL），缺的只是入口。

少了它與線上註冊的賣點直接衝突：整套的重點是「不必到現場」，但要讓一台被撤銷的機器
復活反而只能跑一趟現場匯出 `.req`——因為客戶端看到自己那一列是撤銷狀態就停手，
不會再送申請。**撤銷一台很容易，救回來要出差。**

新按鈕走與待簽發完全相同的路徑（用那一列已存的指紋簽），保護一個都沒少：警告
「這台裝置已被撤銷」＋撤銷時間，不勾「我了解上述狀況，仍要繼續簽發」按不下去。
實測 `ISSUE_COUNT` 1→2——重新啟用算一次換發，清冊上留得住痕跡。


### 換發要真的傳到機器上（2026-08-31 修正）

第 1 步只問「本機這份還驗不驗得過」，驗得過就直接放行——所以**換發對已經在跑的機器
完全沒有作用**。同一台、同一個情境，前後只差有沒有這段程式：

```
修正前  DB 825 字元、帶 2026-09-30 到期日
        本機 794 / 6aa7979e / expiresAt null   ← 一個位元組都沒變
修正後  本機 825 / dfbae725 / expiresAt 2026-09-30
        log「源頭有更新的授權，已換用」
```

主控台改期限、縮短、加註記，簽再多次 `ISSUE_COUNT` 一直往上跳，那台機器完全不知情：
**清冊上看起來都做了，現場什麼都沒變。** 而且**簽過一次永久授權之後永遠不會自癒**，
因為本機那份不會到期——「舊的到期就會換上新的」這個想法對永久授權不成立。

不必多打一支 API：`ReportSeen` 的回應**本來就帶著 `LICENSE_TEXT`**，新的那份已經在
客戶端手上，只是被丟掉了。

**驗得過才換。** 絕不能拿一份壞掉的授權去蓋掉一份正在運作的——那會把人鎖在門外，
而原因（源頭那份有問題）在畫面上完全看不出來。要讓一台停用，正確的工具是**撤銷**，
不是簽一份過期的。

> 順帶記一個時區細節：主控台選 `2026-09-30`，存進去是 UTC 的 `23:59:59`，
> 換算台北是 **10/01 早上 8 點**才失效。方向是寬鬆的（不會提早鎖人），
> 但若有人以為「10/1 零時就停」會差 8 小時。

## 資料表

`HD_DEVICE_LICENSE`（HDPACS，主控台維護）。migration `007` 建表、`008` 加線上派發欄位。

| 欄位 | 用途 |
|---|---|
| `STATUS` | `pending`（看片端送出、待簽發）／`active`／`revoked` |
| `REQUESTED_AT` | 申請時間，也是暫用期的起算點 |
| `LICENSE_TEXT` | 簽好的授權檔內容，看片端線上取回用 |
| `LAST_SEEN_AT` / `APP_VERSION` | 看片端每次啟動回報 |
| `FP_*` | 四項指紋雜湊 |
| `HOSPITAL_NAME` / `DEPARTMENT` / `STATION_NAME` / `DOCTOR_NAME` | 清冊資訊，隨時可改，不進授權檔 |

`STATION_NAME` 與 `MACHINE_NAME` 並存：後者是自動帶回的 Windows 電腦名稱
（常是 `DESKTOP-8K2J1` 這種沒有意義的字串），前者是現場的叫法（「判讀室 1 號」）——
同事回報「判讀室那台不能用」時要靠這一欄才找得到是哪一筆。

一台裝置一列。**換發是更新同一列**（`deviceId` 不變），次數記 `ISSUE_COUNT`。
判斷是不是換發要看 `ISSUE_COUNT > 0` 而不是「這一列在不在」——線上申請已經先佔了一列，
用後者會把第一次簽發記成換發。

---

## 主控台（`/licenses`）

- 頂部顯示待簽發數量；清單把 pending 排最前面（那是唯一需要有人動手的東西）
- 每列一顆「簽發」鈕，不必上傳檔案；誤裝的申請可以「拒絕」
- **批次簽發**：一次把可自動處理的全部簽掉。**指紋與清冊上另一台相符的會被略過**，
  不會硬簽——那種情況只能由人判斷是換機還是誤裝，批次沒有人可以問。
  略過的要走該列的單筆「簽發」，那裡會列出相符的是哪一台並要求勾選確認
- 「本站台的簽發公鑰」可展開——就是看片端要內嵌的那一串。
  沒有它，要回答「看片端裡那串到底是不是這台簽的」就得 ssh 進主機下 openssl，
  而那時候通常已經在查「為什麼授權驗不過」了
- 上傳 `.req` 與線上申請**共用同一段簽發邏輯**，不會一邊改了另一邊沒改
- 「下載」＝用資料庫裡的內容**重新簽**（不是把存的那份吐出來），順便更新 `LICENSE_TEXT`。
  換簽發金鑰之後重新下載會自動用新金鑰，不會拿到一份用舊金鑰簽的死檔

**同一份程式碼靠「私鑰檔在不在」自動切換原廠（可簽）／醫院端（唯讀）**，不必維護兩個版本。

> ⚠️ **但醫院端目前裝不了主控台。** 它的登入是 OIDC 導向 `sso.hdtech.tw` 的 Keycloak，而
> **大部分醫院是封閉網路**（資安要求），連不到就走不到登入頁。原本「醫院端也裝一份唯讀主控台」
> 的規劃在這個前提下不成立，要另外給本機帳號或別的離線驗證方式。
>
> 連帶的是簽發怎麼做：`.191` 連不到醫院 DB、醫院連不到 SSO，**只有工程師的筆電同時碰得到兩邊**。
> 選項與取捨見 backlog REQ-015。三個動作（拉／簽／推）**都不碰醫師的電腦**——
> 那台只在醫師自己登入時送出申請、下次啟動時取回授權。

> ⚠️ 私鑰在 `.191:/etc/hd-admin-console/license-signing.key`，**必須 chown `hdadmin`**。
> 維持 root 會讀不到而靜默退成唯讀模式，畫面上完全看不出原因。

---

## 與舊版（HD.Desktop / .NET Framework 4.8）的差異

舊版在 `C:\Users\yang\source\repos\HD.Desktop`：`HD.Core/Register/`（客戶端）＋
`HD.LicenseCreater/`（產生端，連 HDCRM Postgres）。

| | 舊版 | 新版 | 為什麼改 |
|---|---|---|---|
| 金鑰 | **RSA 私鑰（含 P/Q/D）寫死在客戶端** | 客戶端只有公鑰 | 舊版反編譯即可偽造授權，等於沒有保護 |
| 驗證 | 用私鑰「解密」授權檔 | 用公鑰「驗簽章」 | 能解開不代表是原廠簽的 |
| 演算法 | RSA-1024 | ECDSA P-256 | 1024 以現在標準過短 |
| 指紋 | `MD5(CPU + 主機板)`，任一變動即失效 | 四項取二 | 舊版實際發生過「韌體更新後突然不能用」 |
| 到期 | 硬鎖 | 預警 → 寬限 | 看片軟體半夜被鎖住會出臨床事故 |
| 授權檔位置 | exe 旁邊 | `%ProgramData%` | 新版是版本化安裝目錄，exe 旁邊會被更新／清舊版帶走 |
| 取得授權 | 一律人工帶檔案 | 線上自動，離線是備援 | 醫師電腦連不到，跑現場成本最高 |

**授權檔格式不相容**，新版不讀舊版 license；現場換裝新版時一律重新簽發。

---

## 實測紀錄（2026-08-14）

看片端指向 `.163`（當作醫院主機），簽發在 `.191` 主控台。十項全數通過。

| # | 情境 | 結果 |
|---|---|---|
| 1 | 首次啟動送出申請 | 指紋 4/4；257ms 寫入 pending，`ISSUED_AT` 為 null、`ISSUE_COUNT=0` |
| 2 | 重開 | **不會重複開一台**（靠本機 `device.json` 的 DeviceId 回頭找）；`LAST_SEEN_AT` 更新 |
| 3 | 登入前 | 一行 `[Licensing]` 都沒有——閘門不在啟動路徑上 |
| 4 | 主控台簽發 → 看片端取回 | `STATUS=active`、`ISSUE_COUNT=1`（第一次不算換發）；看片端 51ms 自動落地 |
| 5 | 之後啟動 | 27ms、完全不碰 DB；`LAST_SEEN_AT` 由背景回報晚 5 秒更新（證明沒排在啟動路徑上） |
| 6 | 資料庫沒有授權表 | 安靜退回離線流程，不影響使用 |
| 7 | 關閉 VPN | 停在登入畫面，訊息是「無法連線到伺服器主機」而非授權問題 |
| 8 | 離線匯出／匯入 | `.req` 帶 `existingDeviceId`；簽發端認出是換發；匯入驗簽通過 |
| 9a | 暫用期內 | 提示 + 「繼續」可按 |
| 9b | 暫用期過 | 擋下，停在登入畫面 |
| 9c | 已簽發 | 直接進去，19ms |
| 9d | 撤銷 | 背景回報記下 → 下次啟動顯示寬限期提示、仍放行 |
| 9e | 指紋不符 | `相符 0／可比對 4` → 擋下，**且不覆蓋原本有效的授權** |
| 10 | 安裝包實裝 | `%ProgramData%` 目錄權限為 `Users:(OI)(CI)(M)`；登入後 37ms 自動取回授權 |

機制測試另有 15 項（憑證鏈、域分離、憑證過期、兩層鏈、舊格式相容）與
簽發服務 15 項（裝置 ID 保留、換發計次、`LICENSE_TEXT` 落庫）。

### 測試中抓到並修掉的問題

| 問題 | 原因 | 修法 |
|---|---|---|
| 蒐集階段仍跳註冊視窗 | 「已送出申請」是放行狀態卻帶著訊息 | 沒有授權時一律安靜；有授權但漂移／撤銷才顯示 |
| 按下登入到主畫面明顯變慢 | 閘門移到登入後，WMI 與資料庫往返全擠在按鈕與主畫面之間 | 指紋改在啟動時背景預熱（139ms 移出關鍵路徑）；「表不存在」記住不再問（253ms→41ms） |
| 註冊視窗被登入視窗蓋住 | 登入視窗是 `TopMost`，會把自己拉回最上層 | 開對話框前讓出最上層（照「啟動設定」既有前例）；改 `CenterParent` |
| 「重新檢查」按了像沒反應 | 同步跑在 UI 執行緒，視窗凍住不重繪 | 改背景執行 + 按鈕停用 +「檢查中…」+「最後檢查」時間 |
| 授權檔在選檔視窗看不到 | 主控台產 `.dat`、看片端只認 `.lic` | 統一 `.lic` |
| 「沒有裝置授權表」查不下去 | 只寫結論不寫原因 | 帶上 SqlState、原始訊息、連到哪個資料庫、用什麼帳號 |

---

## 強制模式實測紀錄（2026-08-31）

看片端與簽發都指向 `.191`（`v2.0.41`），跑的是 `ReleaseEnforce` 組建
`2.4.0+20260831-135126+0800.ENFORCE`。**這一輪 `Enforce` 真的是 `true`**——
測的不是「機制會不會動」（那是 08-14 那輪），是「醫院開通那天會發生什麼」。

| # | 情境 | 結果 |
|---|---|---|
| 1 | 沒有授權啟動 | 擋下並跳註冊視窗；暫用期內／過期 × 線上／離線 四種訊息各自不同 |
| 2 | 主控台單筆簽發 | `STATUS=active`、`ISSUE_COUNT=1`、`LICENSE_TEXT` 794 bytes |
| 3 | 批次簽發（5 台待簽發，其中 2 台指紋互撞） | **成功 3 台／略過 2 台**——衝突的沒有被硬簽出去 |
| 4 | 對略過的再按一次批次 | 成功 0 台，結果一致（不是隨機的） |
| 5 | 單筆簽發衝突的那台 | 列出相符的是哪一台（醫院／科室／站別／裝置代碼），要求勾選確認才放行 |
| 6 | 簽發後重新啟動看片端 | **完全安靜地進主畫面**；`license.lic` 794 bytes，與資料庫那份逐字相同 |
| 7 | `APP_VERSION` 回報 | `...+0800.ENFORCE` 共 34 字元**完整**寫入（欄位實查寬度 64） |

第 6 項是這一輪最該測的一項：前面全部在測「沒授權會怎樣」，但**醫院簽發完是不會有人回頭
檢查的**——簽出去卻取不回來的話，沒有任何人會發現，直到暫用期過。

第 7 項順便驗證了 `db_update_v2.0.41`。判準用 `information_schema.columns` 直接問欄位寬度，
不看版本號——理由見[更新鏈分岔](josef-db-upgrade-plan.md)：更新鏈與真實結構會分岔，
而分岔是靜默的。

### 批次簽發：把「略過」講成一個結果，而不是一個待辦

第 3、4 項的行為是對的，但畫面把人導進迴圈：

```
按鈕寫「一次簽發這 5 台」 → 實際簽了 3 台
剩下 2 台仍列在待簽發，按鈕還是寫「這 2 台」
再按一次 → 「成功 0 台」 → 看起來像沒反應
```

改法（主控台 `0.1.0-alpha.29`）是讓文案承認略過是常態：

- 按鈕改成「**一次簽發（可自動處理的 N 台）**」。N 是**這次真的會動**的數量，
  不是清單長度，所以按完就歸零，不會停在原地
- 略過清單底下補一句，明講這幾台再按批次也一樣會被略過，要用該列的「簽發」逐台確認

這句是必要的，因為**兩條路徑的保護程度不同**：批次遇到疑慮只能停手（它沒有人可以問），
單筆才會把衝突對象攤開並要求確認。沒有那句話，人只會反覆按批次。

### 還沒測

| 項目 | 為什麼還沒做 |
|---|---|
| 撤銷後 7 天寬限 | 要撤銷正在用的那台，測完得再簽發回來 |
| 授權複製到別台 → `Mismatch` | 需要第二台實體機器（判定邏輯本身有單元測試覆蓋） |
| 斷網啟動 | 程式碼上是本機驗簽不碰 DB，成本低但沒實跑過 |

---

## 安裝程式

`viewer.iss` 建立 `%ProgramData%\HyperDigital\DicomImageViewer` 並以 icacls 授權 Users 可寫
（提權安裝建出來的目錄一般使用者只能讀，而醫師帳號通常不是系統管理員——
寫不進去會變成「授權拿到了卻存不下來，每次開機都要重新取得」）。

**解除安裝不刪這個目錄**：重裝之後應該要能直接沿用原本的授權。

---

## 第二階段（若要把簽發權下放）

`chain` 欄位與驗證路徑都已經就緒：原廠簽一張有期限的站台憑證給醫院主機，
站台憑證再簽裝置授權。站台憑證**一定要有期限**（與裝置授權相反）——
下放出去的簽發權必須會自己到期，否則憑證一流出去就是永久有效。

目前沒有要做：那等於把「發授權的能力」交出去，而我們的授權本來就只是嚇阻級。

---

## 這個做不到什麼

程式是 self-contained .NET，有心人反編譯後把驗證段拿掉即可繞過。本機制擋的是
「把資料夾複製到另一台就能用」，**不是**防破解。要提高門檻需另外談混淆／加殼。

同理，`%ProgramData%` 的授權檔可被複製到另一台，但指紋不符會被擋下——
除非該機器四項全中，那就是同一台。
