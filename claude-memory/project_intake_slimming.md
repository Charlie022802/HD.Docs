---
name: project_intake_slimming
description: 主PACS進檔瘦身—停存.meta(REQ-006)、移除DicomToImage預轉JPEG(REQ-007)、DicomToVideo去留待議(REQ-008);與A3相依
metadata: 
  node_type: memory
  type: project
  originSessionId: 913e10e5-5ad9-400e-82d5-b8e1163936aa
  modified: 2026-08-05T19:09:06.842Z
---

2026-08-04 使用者新提三項(承接 A3 [[project_main_pacs_coerce_logging]] 停止改檔)。**尚未實作,已寫完整規劃。** **完整設計文件:`D:\Dev\HyperDigital\docs\intake-slimming-design.md`**(含 DB proc 改動表、C# 改動、決策點、建議順序);backlog=`docs\backlog.md` REQ-006/007/008。

**三個並行調查(Explore)結論精華:**
- **REQ-006**:`.meta` 有**兩個**產生點(C-STORE `DicomStoreProcess.cs:120` + DICOMweb STOW `HdPacsStowService.cs:203-207`)、**零讀取路徑**(純只寫不讀保險、DB 零依賴)→ 可安全停產。停產後**兩處無 .Exists 保護會炸**:`ArchiveManager Blob.cs:81`(壓縮打包)、`ArchiveUploadService BackupFile:100`——要一併改不加/不複製 .meta。其餘(nearline/cachedelete/error 回滾)已有保護。
- **REQ-007**:實際生效路徑=`insert_dicom_info` enqueue DICOM_TO_IMAGE→`HD.DicomToImage` 服務→`update_map_job_status` 設 jpeg='Y'。`DicomStoreProcess.cs:289 ImageProcess`+`ImageConvert.cs` 是**死碼**(從未被呼叫)。**jpeg gate 必改三處**:`get_next_map_job 12756-12765`、`check_map_job_enable 10255-10265`、`check_nearline_cache 10329-10334`(只拿 jpeg、留 mpeg4)——否則 STUDY_CLOSE/nearline 永卡。**最大意外**:預生 JPEG 消費者**不只舊 HDWeb**,還有**現役桌面 viewer HD.DicomImageViewer**(ObjectElement LocalJpegPath、PreviewJpegLoader 縮圖)+**HD.MediaPackage** 燒錄(PackageService 696/731)+DB `wadouri_query`/`viewer_station.*`→移除前必須讓這些改即時 render/容忍缺檔(REQ-007 最大工作量)。`deleteOriginDcm`=多幀清理、config 預設 off、與 jpeg 無關、**不動**(移除 DicomToImage 不會刪原檔)。
- **REQ-008**:`dicomMpeg4` 被 C-STORE/C-MOVE 檢索直接消費(`DicomPACSService.cs:54-55` 支援 MPEG4 語法)→**建議保留預轉**(即時轉大檔多幀會拖垮檢索)。拆 jpeg 時保護 video 的點:`check_nearline_cache 10332-10334`、`insert_dicom_info 16027-16053` if/else、`get_next_map_job 12766-12767`(video gate 保留)、`export.insert_package_job 6693`(讀 mpeg4='N' 勿誤動)。

**建議順序**:REQ-006(最獨立純C#)→ 拍板 REQ-008 保留預轉 → REQ-007 分兩半(先做消費端 fallback 再拆 DB gate+下架服務)。**DB 改動走 append-only `db_update_v2.0.(N+1).sql` 用 CREATE OR REPLACE**(見 [[project_main_pacs_deploy]] SQL 同步鐵則)。

**決策(2026-08-04):REQ-007=完整移除。先動 REQ-006。**
**REQ-008 翻案(2026-08-05):原「保留 MPEG4 預轉」→ 改「移除」。** 理由:當年 DICOM_TO_VIDEO→dicomMpeg4 唯一目的=舊 DicomWebViewer 拿不到 frame;經確認 (a) 新 DicomWeb frames 端點能逐格取真實多幀(.191 實測 454 幀 instance:frames/1 裸像素 multipart JPEG Baseline + frames/1、227、454 rendered 皆 200 JPEG 1016x708)、(b) dicomMpeg4 只有舊 viewer 用、無其他消費者(使用者確認)。**移除做法同 REQ-007 route A**:insert_dicom_info 讓 mpeg4/dicomMpeg4 一律標 'N'、不 enqueue DICOM_TO_VIDEO;check_nearline_cache 的 mpeg4/dicomMpeg4 IS NULL gate 因變 'N' 自然放行、不動;下架 hd-dicom-to-video(DicomToVideoService+VideoToDicomMpegService)+ slnx 移除;DicomPACSService MPEG4 語法支援保留(無害);export.insert_package_job 讀 mpeg4='N' 過濾維持有效。**已實作(2026-08-05):** DB commit Database `2449d2a`(db_update_v2.0.27 insert_dicom_info video 分支改一律 'N'、不 enqueue;DB版本.xlsx 2.0.27 加列)+ C# commit HD.Net10 `81d56a6`(slnx 移除+刪整個 HD.DicomToVideo,build 過)。抽出的 proc `D:\HD-Release\test\req008_insert_dicom_info.sql` 供 .191 套用(hd-dicom-to-video 本就沒裝 .191、不用下架服務)。既有卡住 98 個多幀已先手動清成 'N'(MAP_JOB DICOM_TO_VIDEO 98 筆刪除 + RC_OBJECT_CONVERT mpeg4/dicomMpeg4 null→'N')。**待:.191 套 proc(pg_get_functiondef LIKE '%REQ-008%'=t)。進檔瘦身 REQ-006/007/008 全部完成。**
**JPEG 政策更新(2026-08-04):今後取 JPEG 一律走新 DicomWeb(WADO-RS rendered 即時 render)。→ REQ-007 簡化:PACS/DB 端(停預生+拆 3 gate+下架 hd-dicom-to-image+清死碼)可直接做,不必先幫消費端做 fallback;消費端(HD.DicomImageViewer 縮圖/預覽、HD.MediaPackage 燒錄、viewer_station.*/wadouri_query 的 CacheJpegPath)遷移改接 DicomWeb 脫鉤、日後各自做。桌面 viewer=repo `Charlie022802/HD.DicomImageViewer`(獨立 repo,JPEG 消費在 HD.DicomImageViewer.Core/Core/ObjectElement.cs CacheJpegPath/LocalJpegPath + PreviewJpegLoader 等;取值可能經 HD.DicomImageViewer.Server/HD.WebApi)。⚠️ JPEG 全導 DicomWeb 後 REQ-004(DicomWeb 縮圖無快取)重要性上升。**

**REQ-007 ✅ PACS/DB 端完成(2026-08-04,.191 驗證通過,route A):** C# commit `HD.Net10 86ef7bd`(slnx 移除 + 刪整個 HD.DicomToImage 專案 + 清死碼 ImageProcess/ImageConvert.cs/DicomConvertConfig/GetDicomConvertConfig/UpdateObjectConvertStatus/FilePaths.Jpeg,build 過) + DB migration **併入開著未結案的** `db_update_v2.0.27.sql`(本地 canonical `D:\Dev\HyperDigital\Database\HDPACS\db_update_sql\`;2.0.27 是當前開放版本桶、累積多筆帶日期變更,REQ-007 當一筆 20260804 Charlie 加尾) + `DB版本.xlsx` 2.0.27 分頁加一列(新細明體 12、日期整數格式)。**做法**:以 v2.0.25 現行 insert_dicom_info 為底,把 is_convertible 內 DICOM_TO_IMAGE 分支改成「一律 `update_object_convert_status('DICOM_TO_IMAGE',ref,'N')`、永不 enqueue」,video 分支原封不動 → jpeg 永遠非 null → 3 處 jpeg gate 不必改就放行(route A 低風險)。**驗證(.191,admin.clear_study() 清資料後)**:migration_applied=t + toJpeg=**true** 下 C-STORE 新影像 → CONVERT_STATUS jpeg='N'(非 null)、MAP_JOB 無 DICOM_TO_IMAGE、只有 STUDY_CLOSE → 結構性停止、不靠 config。**待辦**:①環境包 `D:\HD-Release\environment\sql\` **已同步到 v2.0.27**(2026-08-04,commit env/sql `e76fe45`,含 Charlie 既有變更 + REQ-007);**.191 已跑完整 `db_update_v2.0.27.sql`、完全跟上 27**(2026-08-04 驗證:get_next_delete_study 存在 + insert_dicom_info 含 REQ-007,皆 t);②消費端(2026-08-04 釐清=併入更大取代,非原地修補):**HD.DicomImageViewer 線上版不可動→JPEG 改 DicomWeb 要另開新專案(平行版,未來軌);HD.MediaPackage 整支待淘汰→改 Export WebApi(=REQ-003,定位提升為全面取代 hd-media-package);viewer_station.*/wadouri_query 的 CacheJpegPath 現由 **hd-web-server(legacy Node web)** 讀預生 .jpg 供給,方向=全面改去 HDDicomWeb 即時 render**。**⚠️硬相依:REQ-007 停預生後 hd-web-server 對新資料無 .jpg → 新版 HD.Net10(REQ-006/007)上線必須與「影像供給改 DicomWeb」同步,否則舊 web 縮圖開天窗;生產 .234 保留舊版故暫無影響,正式 rollout 前提是先讓 DicomWeb 供圖(+REQ-004 快取)並切換來源**。REQ-004(DicomWeb 縮圖快取)重要性上升、且只剩「加快取」一途(選項2 吃預生 .jpg 已死);③resend_convert_dicom_job/restore/archive_decompress 的 DICOM_TO_IMAGE enqueue 是 archive 邊路徑、日後同法停用。C# 端不需重部署(行為全在 DB migration)。

**REQ-006 ✅ 完成(2026-08-04,.191 驗證通過 + commit/push):** commit HD.Net10 `17de498` + HD.Pacs.DicomWeb `8fb0562`。.191 更新 hd-pacs(binary swap 包 `D:\HD-Release\test\hd-pacs-update.tgz` + update-hd-pacs.sh)後 C-STORE 新影像→磁碟不再產 .meta(.meta 總數維持不變、新檔旁無 .meta),舊資料 .meta 保留、DB 正常。以下為改動細節: HD.Net10 5檔(`DicomStoreProcess.cs` 移 SaveMetaFile 呼叫+FilePaths.Meta 欄位;`.FileIO.cs` 移 SaveMetaFile 方法+Meta 路徑+error 回滾 meta 刪;`.Validation.cs` 移孤兒 GetMetaInfo;`ArchiveCompressService.cs`+`ArchiveUploadService.cs` 改「.meta 存在才納入封存/上傳」相容舊資料—**必要**因新資料無 .meta 否則 Blob.ReadAllBytes/File.Copy 炸) + HD.Pacs.DicomWeb 1檔(`HdPacsStowService.cs` 移 STOW .meta 寫出+BuildMetaFile+回滾刪)。**待:.191 驗證(C-STORE 新影像→旁邊不再有 .meta、archive 不報缺檔)+ commit(兩 repo:HD.Net10 + HD.Pacs.DicomWeb)**。REQ-007/008 未動。

**REQ-006 停存 `.meta`:** 進檔不再產 `<ordinal>.meta`(metadata-only DICOM 副本)。`.meta` 是「檔案會被就地改寫」時代的保險,A3 已停改檔→冗餘。移除點=`HD/DicomCore/DicomStoreProcess.cs:120 SaveMetaFile`(路徑 FileIO.cs:23、錯誤清理 :128)。下游會搬/刪 .meta 的:HD.ArchiveManager(Nearline/ArchiveUpload/Compress/Decompress)、HD.CacheDelete—都要容忍不存在。**待確認**:有無「讀」.meta 還原 metadata 的路徑(初掃只見 write/copy/delete)。

**REQ-007 移除 DicomToImage(預轉 JPEG):** `hd-dicom-to-image`(.191 本來沒裝)。預轉 JPEG 是舊 HDWeb WebViewer 用的(舊 web 慢需預轉);新 DicomWeb 即時轉夠快→砍。**牽動廣+與 A3 直接相依**:HDPACS SQL 進檔塞 DICOM_TO_IMAGE job(~4034/9723/16031)、update_object_convert_status、RC_OBJECT_CONVERT jpeg:null 初始化、config `toJpeg` 開關。**⚠️關鍵**:`get_next_map_job` 的 **STUDY_CLOSE jpeg gate(12756-12765、10260、10330-10334)要求所有物件 jpeg 非 null 才放行**——就是今天 .191 靠 `toJpeg=false` 繞過那關;移除 DicomToImage 必須同時拆這些 gate,否則 STUDY_CLOSE 永卡。**⚠️** `deleteOriginDcm`(12429)疑似轉檔後刪原檔流程,移除前查清別誤刪原檔。JPEG 目錄 cacheLocation/JPEG/。**待確認**:除舊 HDWeb 還有誰讀預生 JPEG(REQ-004 縮圖選項2「吃現成 jpg」若砍此流程即作廢,只能走即時 render+快取)。

**REQ-008 DicomToVideo(MPEG4)去留待議:** 轉 MP4 花時間,即時可能來不及,或仍需預轉。**暫不動、先討論**(哪個 viewer/流程在吃 MP4)。與 REQ-007 共用同批 DB proc/convert status/gate(jpeg 與 mpeg4/dicomMpeg4 混在一起),故 REQ-007 拆 jpeg 時**務必保留 video 分支完整**。
