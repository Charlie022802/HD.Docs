---
name: project_main_pacs_coerce_logging
description: 主 PACS(HD.Net10)—出口疊合+A3 停改檔+全九支接集中日誌皆完成部署 .191;fo-dicom client 工廠修+JSON 容錯+P2 連線事件
metadata: 
  node_type: memory
  type: project
  originSessionId: a23fe480-c347-41d9-ba5a-eb699a5d1cf4
  modified: 2026-08-09T17:46:24.647Z
---

**✅ 現況(2026-08-10):A 系列全完成**(A0~A3 含停止改檔,.191 驗證過)。**全服務接集中日誌**(`d6f831b`:七支補 ConfigureLoggingPlatform,加上原有 PACS/Worklist=九支全送 App=HDPacs;unit 都掛 `/etc/hd/logplatform.env`)。**接上日誌十分鐘揪出兩蟲並修**:①fo-dicom 5 DicomClient 直接 new 必 NRE→六呼叫點改 DicomClientFactory(`a08bde4`,詳 [[reference_fodicom5_client]]);②DicomJsonConverter 數值容錯+MapJobManager 單筆隔離(`c5c1a92`,STUDY_CLOSE 佇列曾被一顆壞 dataset 卡死);另 HandleStorageError dynamic→具名 tuple(`e3cb1ef`)、watchdog bypass AE(bmDshOPXxzCcsa39)不發連線事件(`8d824af`)。**P2 連線事件**見 [[project_loggingplatform_product_zones]]。**.191 更新慣例**:tgz(services/<服務名>/=publish 產物)+`deploy/update-services.sh`(HD.Net10 repo,`33a9779`;停止→備份→覆蓋保留 appsettings→重啟);.191 dotnet 在 `/opt/dotnet`、服務在 `/home/HD/service`、已裝:pacs/worklist/callback/dicom-service-manager/dicom-transmit/media-package/workflow-manager(+web-server)。

主 PACS **HD.Net10**(多專案 slnx,fo-dicom 5.2.5,Npgsql 8,HDPACS DB)兩條工作線,計畫 2026-08-03 核准(計畫檔 `C:\Users\yang\.claude\plans\pure-splashing-snowglobe.md`)。承接 [[project_immutable_original_coerce]](DicomWeb WADO 試點已上線)與 [[project_shared_logging]]。

**決策(已確認):** ①出口疊合**試點先行**(先 DIMSE C-STORE);②**所有出口驗證後**才砍 `UpdateDicomFileSafe`;③日誌 **DIMSE 監聽優先**(HD.PACS+WorklistServer)。

**關鍵架構:** 全系統實體路徑經 `get_next_map_job`→`MapJobInfoData.filePath`,`includeDataset=true` 時 `MapJobInfoData.dataset`=DB 校正後 DICOM-JSON(天然疊合切入點)。C-MOVE 在 HD.PACS 只 `insert_job_queue` 不讀檔、C-GET 停用,真正讀檔送出在 **HD.DicomTransmit**。DB reconcile(`update_study_info` 反灌 RC_OBJECT.DATASET)全在 DB 端、不在 C#,砍 C# 改檔不影響它。

**已 commit(2026-08-04):** HD.Net10 `eb7f932`(A0/A1/A2/B + REQ-005 fix,12 檔,master)、HD.LoggingPlatform `8915f84`(ClefParser @l fix,master)。皆已 push origin master(GitHub Charlie022802/*)。未部署到正式(僅 .191/.195 測試)。A3 仍待做。

- **A3 驗證通過(2026-08-04, .191):** 部署 `hd-workflow-manager`(先前 6 支子集沒裝這支!當初刻意排除改檔 worker)。包=`D:\HD-Release\test\hd-workflow-manager.tgz`(publish linux-x64 + `install-workflow-manager.sh` 加裝到既有 /home/HD/service、沿用 hd_conf.json、appsettings 只開 StudyClosedService 其餘關)。**測法**:前置 `skipWorklistVerified=true`(讓 study_fix_up_auto 直接放行)+ `AE_CONFIG toJpeg=false`(.191 沒裝 hd-dicom-to-image,否則新影像掛待轉 jpeg→STUDY_CLOSE gate 12756-12765 永卡)+ C-STORE 一份 study(STUDY_REF=1, 11物件, jpeg_null=0)。設 `RC_STUDY.INSTITUTION_NAME='A3-TEST-INST-001'`(reconcile update_study_info 會把它疊進 DATASET 00080080、又必與磁碟原檔不同)→ 塞 STUDY_CLOSE(modifiedFiles=true,is_immediate=true;既有 job dedup「Already exist」但標記在領 job 時才讀故仍生效)→ 啟動服務→log `Start/Complete Close Job [1] (Study:1)` 80ms。**結果**:檔案 md5 `1656dcaa…6ff3` before==after **一字不差**(舊碼因 InstitutionName 不同必重寫、md5 會變)→ **停止改檔證實**;DATASET 00080080=標記但檔案沒有→校正只在 DB;STATUS→X 正常關閉;MAP_JOB 0筆無迴圈殘留。無下游扇出 job=.191 沒設 routing/archive/nearline/callback,非問題。踩雷:install-hd.sh 建 unit 的 `StartLimitIntervalSec` 放在 [Service] 被 systemd 忽略(應在 [Unit]),無害待修。
- **A3 DONE(2026-08-04, build 過、未部署未測):** `HD.WorkflowManager\Service\StudyClosedService.cs` 砍掉就地改檔——移除 `UpdateDicomFileSafe`、兩呼叫點(來源+MPEG4)、孤兒 `DatasetsAreEqual`、死掉的 `tempInfo`/`TempPath`/相關 usings(FellowOakDicom/HD.DicomCore/System.IO/System.Linq)。**保留** job 迴圈 + `RestartUncompletedJobs`/`UpdateProgess`/`UpdateDone`/`UpdateError`(進度/完成標記照舊→下游 `study_closed()` 扇出不受影響)。`GetJobs(..., true)` 的 includeDataset 不動→`get_next_map_job`→`update_study_info` 反灌 RC_OBJECT.DATASET 的 DB reconcile 仍在 DB 端跑。STUDY_CLOSE 現在只推進狀態、不碰原檔;校正全靠 DB overlay + 出口疊合。**待 commit + 部署 + 測。**

**進度(2026-08-03):**
- **A0 DONE(build 過):** 新增 `HD\DicomCore\CoercionService.cs`(namespace HD.DicomCore)。移植 DicomWeb 疊合:`ApplyCoercion(target,dbOverlay,studyUid,seriesUid)`(逐 tag AddOrUpdate、跳 PixelData/group0002、父表 UID 強蓋、每 tag try/catch 寬鬆) + `CoerceBySopUid(target,sopUid)`(用 `PostgresConnection` 以 `json_build_object` 一次撈 RC_OBJECT.DATASET+RC_STUDY/RC_SERIES UID,`ParseDatasetLenient` 寬鬆解析)。重用 `HD.Database.PostgresConnection`、`HD.DicomCore.DicomDatasetExtension.ToDicomDataset()`。踩雷:`Log` 撞 `HD.Log` namespace,要寫 `Serilog.Log.Logger`。
- **A1 DONE(build 過,未部署未測):** `HD.DicomTransmit\Service\DicomTransmitService.cs`(CSTORE/CMOVE/ROUTE 共用 ExecuteAsync)。job 迴圈前建 `CoercionService`;每個 currentDicomFile 開檔+NotValidated 後、讀 UID/匿名/轉檔前疊合(優先 `mapJobInfoData.dataset`,無則 CoerceBySopUid)。
- **A2 DONE(全 slnx build 過,未部署未測):** 對外出口用 `CoerceBySopUid`(SOP UID 直接從開出來的 dataset 取)。①`HD.MediaPackage\Service\PackageService.cs`:job 迴圈建 coercionService;full-package(讀 SOP 後)、onlyJpeg(開檔後)、cover-sheet(dcmTemp 開檔後)三處疊。②`HD.CallBack\Service\StudyCallbackService.cs`:wanFang 分支 `dt` 讀 tag 前疊 + 傳 `commonSetting.Database` 給 handler。③`HD.CallBack\ChangGung\ChangGungHorizonHandler.cs`:建構子加 `DatabaseConfig`(選填)、HandleDicom 開檔後疊。**VideoToDicomMpeg 決定跳過**(產出是內部衍生檔,經 DicomTransmit/WADO 真正出去時才 coerce;DB reconcile 也會校正其 RC_OBJECT.DATASET,retrieval 自然疊)。archive/nearline=原檔不疊;純像素 JPEG/MP4 metadata 不影響像素(MediaPackage onlyJpeg 已順帶疊 burn-in 用 dataset)。
- **.191 部署 DONE(2026-08-03):** 清空重裝環境(ENV 1.0.4,.NET 10.0.10)+ 新版 HDPACS 首裝(`hd-new-install.tgz` 6 支子集,install-hd.sh),**6 支全 active**(pacs/worklist/transmit/callback/media-package/service-manager)。日誌 env 已設(/etc/hd/logplatform.env→.195)。**功能驗證待做**(fresh DB 無資料,要先 C-STORE 測試影像進去)。
- **B 日誌驗證通過(2026-08-04, .191):** C-ECHO→.195 收到 `CEcho Request`,properties 帶 `User=TESTSCU`+`ClientIp=192.168.68.253`(真實來源 IP),平台 render 成標籤。enrichment(LogScope.Dimse 在 handler 內)正常。注意:**只有「我在 handler 內寫的 log」帶 ClientIp/User;fo-dicom 內部 log(SourceContext=FellowOakDicom.Network,如 C-Store response)在 handler scope 外、不帶**——可接受(那些是 fo-dicom 細節)。**踩雷→發現 LoggingPlatform bug:** 顯示「Verbose」是假的——`ClefParser` 缺 `@l` 時 evt.Level 停在 short 預設 0=Verbose,但 CLEF 規範 @l 省略=Information(formatter 對 Information 不寫 @l)。**已修 ClefParser.cs(缺@l 預設 Information)、commit `8915f84`、已部署 .195(2026-08-04)**:只重 build Ingest(ClefParser 在 Shared 但等級於 ingest 解析時決定)→ podman load 覆蓋 hdlog-ingest:v1.0.0 → `podman compose up -d --no-deps --force-recreate ingest`;只影響之後進來的 log。影響所有產品(DicomWeb Info log 也一直被誤標)。sink Information 過濾有效、無 flooding。
- **A2 coerce 驗證通過(2026-08-04, .191):** 媒體燒錄路徑。手法:改測試 study RC_OBJECT.DATASET 的 InstitutionName(0008,0080)=`COERCE-OK-8842`(只寫 DB、沒觸發 StudyClose)→ `export.insert_package_job` 建 job(踩雷:accession 尾端有空白比對不到→改用 studyRef+patientId+ignoreMultiframe:false;study 不需 close)→ hd-media-package 產出 7 個 .dcm 到 burnTemp/1/DICOM/ → **每檔都含 COERCE-OK-8842(命中7),磁碟原檔沒有** → coerce 疊進媒體輸出、原檔不變。A1 同用 CoercionService.ApplyCoercion,機制已證。**注意:** 用 PatientName 驗會被 PackageService line 278-279 的 studyInfo 覆蓋蓋掉,故改用 InstitutionName 這種 studyInfo 不碰的欄位。
- **發現獨立 bug(非 coerce):** MediaPackage 燒錄的「建 study-elements JSON(光碟 viewer metadata)」步驟(PackageService.cs:383,`HD.MediaPackage.Dicom.DicomJsonConverter` 序列化含 PersonName 的 dataset)→ fo-dicom `DicomEncoding.GetStrictEncodings` 對 **ISO_IR 192 中文名 NRE**。台灣每份都中文名,恐燒錄全掛(fo-dicom4→5 重寫 regression);DICOM 檔本身已存出(在 NRE 前),故不影響 coerce 驗證。已記 backlog(REQ-005)。**已修+驗證(2026-08-04)**:`HD.MediaPackage\Dicom\DicomJsonConverter.cs` 兩處(`WriteEncodingJsonElement` 的 elem.Length、`WriteJsonPersonName` 的 Buffer.Data)會觸發 fo-dicom string→bytes 編碼→ISO_IR 192 PersonName NRE;改讀 bytes 包 try、遇 lazy 元素 NRE 退回 Get<string[]>() 直取字串。重測 jobRef 3 燒錄跑完、study_elements.json 產出、marker 全中。fix 在原始碼待隨主批 commit。
- **待辦:** A1(C-MOVE/route runtime,同 CoercionService 高信心)+ callback coerce 的 runtime 驗證(選),都改測試 study RC_OBJECT.DATASET→出口姓名已校正、原檔未動;.195 收到 log 帶 ClientIp/User。**全驗證後** A3(移除 `StudyClosedService.UpdateDicomFileSafe` 111-188 + 呼叫點 72/79 + 孤兒 DatasetsAreEqual 193-239,保留 job 迴圈/DB reconcile)。
- **B 日誌 DONE(全 slnx build 過、無 NU1605,未部署未測):** ①`HD.csproj` Serilog 4.0.1→**4.3.1** + 加 ProjectReference `..\..\HD.Shared\src\HD.Shared.Logging`(放 HD 專案→全服務可透過 HD 取用,現只 PACS/Worklist 接線)。②新增 `HD\Log\LoggingPlatformSetup.cs`:擴充 `ConfigureLoggingPlatform(cfg,config,app)`(讀 GetSection "LoggingPlatform"+env LOGPLATFORM_URL/LOGPLATFORM_API_KEY(後備 INGEST_API_KEY),IngestUrl 空=不送) + `LogScope.Dimse(clientIp,user)`(綁兩個 LogContext.PushProperty)。③`HD.PACS`/`HD.WorklistServer` 的 Program.cs UseSerilog lambda 加 `.Enrich.FromLogContext()`+`.ConfigureLoggingPlatform(ctx.Configuration,"HDPacs")`+`using HD.Log;`。④DIMSE handler 開頭插 `using var _logScope = HD.Log.LogScope.Dimse(RemoteHost, <CallingAE>)`:PACS 的 OnCEcho/OnCStore/OnCFind/OnCMove(user=CallingAEConfig?.aeTitle)、Worklist 的 OnCEcho/OnCFind(user=CallingAETitle)。用 `using var`(單行、iterator 安全)。**注意:** IAsyncEnumerable handler 的 per-yield log 因 AsyncLocal 邊界可能不帶屬性,主要 log(查詢/計數/錯誤)在首段有帶,可接受。**待:** 部署需設 env LOGPLATFORM_URL/LOGPLATFORM_API_KEY(systemd EnvironmentFile,比照 DicomWeb;留空=不送);.195 驗收到 log 帶 ClientIp/User。範本:HD.Pacs.DicomWeb Program.cs。
