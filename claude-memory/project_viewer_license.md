---
name: project_viewer_license
description: "看片端授權(註冊)機制—線上註冊(DB當信箱)+批次簽發全部完成;強制模式(Enforce=true)已在 .191 實測七項全過;出貨版仍是 Enforce=false,何時翻未決"
metadata: 
  node_type: memory
  type: project
  originSessionId: 13e6b6ed-984d-4c27-aed8-2170077bfa02
  modified: 2026-08-14T05:08:13.678Z
---

**看片端授權機制(REQ-012,正本 `docs/viewer-license-design.md`)。✅ 2026-08-14 全部完成並端到端驗證通過。**

**最重要的一個轉折:從離線改成線上註冊。** 原設計是帶 `.req` 回原廠簽、再帶授權檔回現場;但痛點不在簽章,而是「**醫師的個人電腦我們連不到,拿檔案一定要跑現場**」。關鍵觀察:**看片端本來就要連醫院主機的 PostgreSQL 才能看片 ⇒ 能看片就一定連得到 DB**。於是拿 `HD_DEVICE_LICENSE` **當信箱**:看片端 INSERT 申請 → 我們 VPN 進 .191 主控台按簽發 → 授權字串寫回 `LICENSE_TEXT` → 看片端下次啟動自己取回落地。**不開埠、不加服務、不必在每間醫院部署主控台。** 討論時要點破的是:「傳輸怎麼走」與「誰拿私鑰」是**兩個獨立問題**——傳輸改了,私鑰仍只在原廠,因為寫回去的必須是原廠簽的字串,自己塞的驗不過(那張表只是郵筒不是核准權)。離線流程完整保留當備援,兩條路格式相同。

**⚠️ 目前 `LicenseGate.Enforce = false`(蒐集階段,不擋人)。** 現場既有看片端全是「從來不必註冊」的狀態,一上版就擋等於自己把所有醫院鎖住。流程:先出 `false` 的版本讓清冊自動長出來 → 把在用的機器全簽掉 → 下一版才改 `true`。刻意做成**編譯期常數不是設定檔開關**(設定檔裡放「把授權關掉」的開關比不做還糟)。**`true` 的那條路已經用 `ReleaseEnforce` 組建在 `.191` 完整測過(見本文末),剩下的只是決定何時翻。**

**實作時發現的三個真問題(都是照原設計會出事的)**
1. **`Win32_Processor.ProcessorId` 是型號層級的值**(實測 `178BFBFF00B60F00`),同款 CPU 全都一樣 → 複製到**同型號**的另一台會中一項 → 原規則判成「漂移→放行」,而同型號正是醫院整批採購的常態。改列**弱識別**:只中它一項時當作不同機器(但若可比對的只有那一項,仍放行)。
2. **主控台 `IssuedAt`/`LastIssuedAt` 是非 nullable**,pending 列一進來 Dapper materialization 直接炸,**整頁打不開**;`ORDER BY LAST_ISSUED_AT DESC` 也會把 NULL 排最前面。(是使用者問「待簽發頁應該還沒上 .191 吧?」才想到我剛塞的 pending 列會弄壞線上的舊版。)
3. **換發判定原本看「這一列在不在」**,但線上申請已先佔一列,第一次簽發會被記成換發 → 改看 `ISSUE_COUNT > 0`。

**格式(`HD.Shared.Licensing`,簽發端與驗證端共用同一份)**:ES256;**簽的是 payload 原始位元組**(不是解析後重新序列化,否則 JSON 空白/順序有差就驗不過);加 `typ` 做**域分離**(舊授權檔沒有這欄視為 license,仍驗得過);**`chain` 憑證鏈驗證路徑已寫通但沒在用**——現在做幾乎免費,等授權都發出去才做的話所有既有看片端會全部驗不過。15 項機制測試。

**看片端判斷順序**:①本機 `%ProgramData%\HyperDigital\DicomImageViewer\license.lic` 驗簽+四取二(**不碰 DB**,實測 30ms)②DB 用 DeviceId(沒有才用指紋,且只認 Match)找自己那列,已簽發就抓回落地(**重灌/重裝/誤刪自動復原**)③沒有就 INSERT pending ④DB 不通走離線匯出/匯入。**任何一步失敗都只安靜退回下一條**(含 42P01 表不存在=舊環境沒跑 migration)。暫用 72 小時,線上以 **DB 的 `REQUESTED_AT`** 為準(記本機的話重灌就能刷新=無限試用)。撤銷靠**背景回報** `LAST_SEEN_AT`,發現時畫面已開起來,所以記本機、**下次啟動**才處理+7 天寬限(硬關正在判讀的視窗是臨床風險)。

**正式公鑰已內嵌** `LicenseTrust.RootPublicKeyBase64`(`MFkwEwYH...o9ds8Q==`),來源 `.191:/etc/hd-admin-console/license-signing.key`(**確認過 `hostname -I` 是 .191**,不是 .199)。主控台 `/licenses` 新增可展開的「本站台的簽發公鑰」,以後對「看片端內嵌的是不是這台簽的」不必再 ssh 下 openssl。DEBUG 組建才吃 exe 旁的 `license-public.pem`(Release 也吃的話,任何人放自己的公鑰就能自簽)。

**⚠️ 閘門掛在「登入成功之後」不是程式啟動時**(2026-08-14 使用者點破):資料庫位址是**登入畫面的齒輪**在設定的,全新安裝 `localconfig.json` 位址刻意留空 → 放在 Main 就等於「第一次啟動必然走連不上那條」,而且 `PostgresSettings` 預設 Host 是 `127.0.0.1`,會卡滿 10 秒逾時才出現登入畫面。改掛 `LoginForm.LogIn()` 裡帳號驗過+`RefreshConfigs()` 成功之後(Core 只留 `Func<IWin32Window,bool>` 掛鉤,機制在 exe 那層因為要 `System.Management`)。**代價是把 WMI+DB 擠到「按下登入」那一刻**——使用者最有感的位置,實測體感明顯變差 → 指紋改**啟動時背景預熱**(139ms 移出關鍵路徑)+「表不存在」記住不再問(253→41ms)。**副作用:關 VPN 測不到離線註冊**,因為登入本身要連 DB,登入先失敗就走不到閘門;離線那條真正的觸發條件是「DB 通但沒有授權表」。

**十項實測全過(2026-08-14,看片端指向 .163 當醫院、簽發在 .191)**,抓到六個只有實裝才現形的問題,全修掉:①蒐集階段仍跳註冊視窗(「已送出申請」是放行卻帶訊息)②按下登入變慢(見上)③**註冊視窗被登入視窗蓋住**——`LoginForm` 是 `TopMost`,給對話框擁有者不夠,要照程式裡既有前例(「啟動設定」按鈕)先 `TopMost=false` 再 `finally` 還原;順便改 `CenterParent`(多螢幕)④「重新檢查」同步跑在 UI 執行緒→凍住像沒反應,改背景+「檢查中…」+「最後檢查」時間⑤主控台產 `.dat` 但看片端只認 `.lic`(統一 `.lic`,主控台 alpha.4)⑥「沒有裝置授權表」只寫結論不寫原因,測試中真的遇到表消失卻查不下去 → 帶 SqlState+原始訊息+連到哪個 DB/帳號。

**順手修掉的既有 bug(不是這次改出來的)**:連不到主機時跳「使用者[XXX]登入錯誤」——連線失敗與密碼錯在上層都只是 `Success=false`,現場會照著訊息去查帳號而問題在網路;而且要等 **21 秒**(作業系統重送 SYN)。改:`LoginResult.Unreachable`+登入前先探 TCP 埠(1.5 秒,**不用 ICMP ping,醫院常擋**)+訊息改「無法連線到伺服器主機(位址)」+不清空密碼欄。**同時移除直連資料庫的登入路徑**(`SystemConfig.CheckUserPassword`,`DownloadHost` 是 localhost 時原本會客戶端自己比對 BCrypt)——驗證帳密是伺服器的職責。**⇒ `DownloadHost` 現在是必填**,登入一律走 WebApi。

**測強制模式**:DEBUG 組建吃 `HD_LICENSE_ENFORCE=1`(Release 不吃),免得每次改常數又忘了改回來。

**端到端實測(對 .191)**:申請 257ms 寫入 → 第二次啟動**不會重複開一台** → 主控台簽發 → 看片端自己取回落地判定 Valid → 之後每次 30ms 且不碰 DB → 餵一份別把金鑰簽的授權回 `BadSignature`。另有簽發服務 15 項測試。

**⚠️ 簽發在封閉網路下還沒解(backlog REQ-015,2026-08-14 裝機前才確認)**:`.191` 主控台**連不到醫院 DB**、醫院主機**不對外連不到 sso.hdtech.tw**(大部分醫院都是封閉網路,資安要求),**只有工程師筆電同時碰得到兩邊**。**連帶推翻「醫院端也裝一份唯讀主控台」的既有規劃**——它的登入是 OIDC 導 Keycloak,封閉網路走不到登入頁,要另給本機帳號。三個選項:①主控台跑筆電(最快但私鑰要複製到個人機器,此性質先前已否決)②**拉/簽/推**(筆電小工具讀醫院 DB 待簽發→在 .191 簽→寫回醫院 DB;私鑰始終在 .191 不綁人)③純手工(測試時的做法,十台內可行)。**三個動作都不碰醫師的電腦**。不急著決定:蒐集階段不擋人,等清冊長出來看實際幾台再定。

**⚠️ 部署前提:`Enforce=true` 的版本不能進到「沒有 `HD_DEVICE_LICENSE`」的站台。**

**2026-08-31 更正:判準是「表在不在」,不是版本號。** 前一天我寫成「DB < `v2.0.29` 就不行」
並舉若瑟為例,**那是錯的** —— 實際查過:**若瑟(`v2.0.22`)那張表是存在的**,
2026-08-19 前後被單獨補上去、沒走更新鏈(同期的 `SITE`/`PACKAGE_JOB`/
`HD_USER_AUDIT_LOG`/`UPS_WORKITEM` 都還沒有,所以確實只補了這一張)。
**用版本號判斷會得到假的否定**,要用 `SELECT to_regclass('public."HD_DEVICE_LICENSE"')`。
這是 [[project_db_chain_drift]] 的形狀又一次現形。

三台實查(2026-08-31):`.191` = `2.0.41`/有表/寬度 **64**;`.163` = `2.0.26`/有表/32;
若瑟 = `2.0.22`/**有表**/32。**三台都有**,所以強制版目前不會踩到「無法註冊」那條路。

底下那段「表不存在會怎樣」仍然成立,只是**目前沒有站台處於那個狀態**:
鏈是這樣:查不到(表不存在被 catch 吞掉)→ `SubmitRequest` 也寫不進去 → 走離線流程在本機建
`DeviceId`+`RequestedAt` → 給 **72 小時暫用** → 之後 `Enforce=true` 就擋下,
**而「怎麼註冊」那條路根本不存在**。不是永久鎖死(重灌會刷新本機 `RequestedAt` 再拿 72 小時),
但等於要現場**每三天重灌一次看片端**。

**症狀形式是「裝上去三天後醫師突然登不進去」**,沒有人會聯想到是 DB 版本 ——
所以這條要當**部署前提**寫死:出 Enforce 版本之前先確認目標站台**那張表在不在**
(`to_regclass`,不是比版本號——見上面的更正)。
若瑟的 DB 升級是使用者 2026-08-26 決定暫停的那件事,見 [[project_josef_site]]。

**「把 `TrialHours` 72 小時拉長到 14 天」已完成**(常數改名 `TrialDays`)——
站台還沒準備好時三天太短,兩週才有緩衝。

**`APP_VERSION` 欄位寬度(2026-08-31,`db_update_v2.0.41`)**:從 `varchar(32)` 加寬到 64。
32 對現在的版本字串已經不夠而且**溢位是靜默的**:`.ENFORCE` 版 34 字元被截成 `.ENFOR`、
`-beta.N` 版 33 字元掉最後一個字 —— 掉的是尾端,正是分辨兩顆 build 的那部分。
**客戶端要同時對付 32 與 64**:先送 64,遇到 `22001` 記住並退回 32 重試一次
(`LicenseRepository._appVersionMax`)。不能讓 `22001` 掉進通用 catch ——
那會讓 `ReportSeen` 回 null、上層當成「查不到這一列」,`LAST_SEEN_AT` 就靜默停止更新,
而那正是判斷「哪些機器還在用」的依據。若瑟與 `.163` 都還是 32,所以這條退路是活的。

**✅ 強制模式實測完成(2026-08-31,對 `.191`/`v2.0.41`,跑 `ReleaseEnforce` 組建)。**
測的不是「機制會不會動」(那是 08-14 那輪),是「醫院開通那天會發生什麼」。七項全過:
沒授權擋下並跳註冊視窗 → 單筆簽發 → **批次 5 台(其中 2 台指紋互撞)= 成功 3/略過 2** →
對略過的再按一次仍是 0(結果一致不是隨機) → 單筆簽發衝突那台會列出相符的是哪一台並要求
勾選確認 → **簽發後重開看片端完全安靜地進主畫面**(`license.lic` 794 bytes,與 DB 那份逐字相同)
→ `APP_VERSION` 34 字元完整寫入。

**其中「簽發後取得授權」那項最該測**:前面全部在測「沒授權會怎樣」,但**醫院簽發完是不會有人
回頭檢查的** —— 簽出去卻取不回來的話沒有人會發現,直到暫用期過。

**批次簽發抓到的 UX 問題(已修,主控台 `0.1.0-alpha.29`,commit `ea63149`)**:行為是對的但畫面
把人導進迴圈——按鈕寫「一次簽發這 5 台」實際簽 3 台,剩 2 台按鈕還是寫「這 2 台」,再按得到
「成功 0 台」像沒反應。改成「一次簽發(**可自動處理的** N 台)」+ 略過清單底下明講「再按一次
仍會被略過,請用該列的『簽發』逐台確認」。**理由是兩條路徑的保護程度不同**:批次遇到疑慮只能
停手(它沒有人可以問),單筆才會把衝突對象攤開並要求確認——沒有那句話,人只會反覆按批次。

**`v2.0.41` 閉環**:`APP_VERSION` 實查寬度 64、`.ENFORCE` 版 34 字元完整沒被截。
**判準用 `information_schema.columns` 直接問欄位寬度,不看版本號**(見 [[project_db_chain_drift]])。

**還沒測**:①撤銷後 7 天寬限(要撤銷正在用的那台,測完得再簽發回來)②授權複製到別台 →
`Mismatch`(需要第二台實體機器;判定邏輯本身有單元測試)③斷網啟動(程式碼上是本機驗簽不碰 DB)。
**`Enforce` 何時對正式版翻成 `true` 仍未決定**——出貨版目前還是 `false`。
**醫院開通要方便**:「批次簽發」與「暫用期 72 小時→14 天」**兩件都已完成並實測**,
兩者都不動信任模型;**刻意沒做「開通期間內自動核發」**——那等於把簽章的意義抽掉。
第二階段(把簽發權下放醫院主機)`chain` 已就緒但刻意不做。

**✅ 撤銷這條路補完(2026-08-31,viewer `1d64052`／主控台 `alpha.30` `6fb3fdd`)。三個洞:**

① **撤銷慢一輪。** 第 1 步(本機授權檔有效)直接 return,回報是 fire-and-forget。同一份 log 的對照組:
`17:31:13.600 授權判定:Valid(放行)` → `17:31:13.621 回報時發現授權已撤銷`——**答案不是太慢,
是沒有人等它**。現場表現是「撤銷完那台照樣能用,要關掉再開一次才擋得住」。改成同步。
「硬關正在判讀的視窗是臨床風險」那個理由**對執行中成立、對閘門不成立**(閘門跑在登入按鈕上,
主畫面還沒開),而且閘門跑在一次成功登入之後,同一台伺服器幾百毫秒前才回應過。
**實測代價 14ms**(判定 47~52ms vs 原本 27ms),登入本身是 400ms 等級。

② **撤銷是單向的。** 本機 `RevokedAt` 只會被寫入、沒有任何地方清掉,而第 1 步一看到就攔截 →
主控台重新簽發之後客戶端**永遠回不來**,**連「匯入授權檔」都救不回來**(新授權檔寫進去了,
撤銷記號還在),現場唯一解法會是「手動去 %ProgramData% 刪檔」。改成 `SyncRevocation` 雙向。
**問不到就維持現狀**——拔網路不能等於解除撤銷,網路一抖也不能把所有人鎖在門外。

③ **主控台清單上沒有「重新啟用」入口。** 但那明明是預期能力(簽發路徑特地處理 `targetRevoked`、
SQL 明確清 `REVOKED_AT`),缺的只是按鈕。**與線上註冊的賣點直接衝突**:整套重點是「不必到現場」,
但要救一台被撤銷的機器反而只能跑現場匯出 `.req`(客戶端看到自己是撤銷狀態就停手不會再送申請)
——**撤銷一台很容易,救回來要出差**。新按鈕走與待簽發相同路徑,保護沒少(警告+勾選確認),
`ISSUE_COUNT` 1→2 留痕跡。

**④ 換發傳不到機器上(`db4c616`)。** 第 1 步只問「本機這份還驗不驗得過」,驗得過就放行 →
**換發對已經在跑的機器完全沒有作用**。實測:DB 825 字元帶 2026-09-30 到期日,本機
794/`6aa7979e`/`expiresAt null` **一個位元組都沒變**;修正後換成 825/`dfbae725`。
也就是改期限、縮短、加註記,簽再多次 `ISSUE_COUNT` 一直跳,那台機器完全不知情——
**清冊上看起來都做了,現場什麼都沒變**。**而且簽過一次永久授權之後永遠不會自癒**
(本機那份不會到期,我原本以為「舊的到期就會換上新的」對永久不成立)。
修法零額外往返:`ReportSeen` 的回應本來就帶 `LICENSE_TEXT`,只是被丟掉。
**驗得過才換**——不能拿壞掉的授權蓋掉正在運作的;要停用該用撤銷。
時區細節:主控台選 `2026-09-30` 存成 UTC `23:59:59` = 台北 **10/01 早上 8 點**才失效。

**四段實測全過**:撤銷後第一次啟動就跳警告(52ms)/寬限期警告視窗/主控台重新啟用/
客戶端自己解除記號(47ms)。

**教訓**:加三語 resx 的腳本在日文字上被主控台 cp950 擋掉、迴圈中斷,`zh-Hans` 漏掉,
**而 build 照樣 0 警告 0 錯誤**——少一個資源只會退回顯示 key。事後單獨數三個檔案才抓到。

相關:[[project_hd_admin_console]]、[[project_viewer_install]]、[[project_auth_keycloak_plan]]
